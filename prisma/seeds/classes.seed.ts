/**
 * Seed de classes — Scrumban-Backend-V2.
 *
 * IMPLEMENTAÇÃO COMPLETA em F1 (sub-plano `01-FUNDACAO.md`).
 *
 * Estrutura final esperada:
 *   classes = [...classesFixas, ...classesEspecificas]
 *
 * classesFixas (45) → vem de `templates/classes-base-template.ts` (range -1..-110)
 * classesEspecificas (~70) → este arquivo (range -150..-529)
 *
 * Total: ~120 classes (ver §3.2 do `00-PLANO-MESTRE.md`).
 *
 * Conteúdo planejado de classesEspecificas:
 *   -150..-159  DEntidade sub-tipos (USER, PLATFORM, ORG, AGENT, ...)
 *   -160..-179  Vínculos Org/Project (cargos)
 *   -180..-199  Outras DEntidade especiais (TEAM, ...)
 *   -300..-319  DPedido (EXECUTION + LOW/MED/HIGH)
 *   -400..-419  Sprints (DTabela)
 *   -420..-429  Priorities (DTabela)
 *   -430..-439  Task Types (DTabela)
 *   -440..-449  Status Intentions V3 (DTabela)
 *   -450..-469  Channels (DTabela)
 *   -470..-489  Configs/Tokens (WEBHOOK, API_KEY, MCP_KEY, INSTALL_TOKEN, PAIRING_TOKEN, ISSUE_COUNTER)
 *   -490..-509  Eventos (NOTIFICATION, WEBHOOK_ATTEMPT, AGENT_HEARTBEAT, ...)
 *   -510..-529  Status lookups secundários (Agent + Execution + Risk levels)
 */

// import { classesFixas, type DClasseSeed } from '../../templates/classes-base-template';

// const classesEspecificas: DClasseSeed[] = [
//   // F1 popula tudo aqui — ver `00-PLANO-MESTRE.md` §3.2
// ];

// export const classes = [...classesFixas, ...classesEspecificas];

console.warn('[classes.seed] Placeholder F0 — implementação completa em F1.');
